<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 字体大小子菜单 -->
    <item
        android:id="@+id/menu_text_size"
        android:title="字体大小"
        app:showAsAction="never">

        <menu>
            <item
                android:id="@+id/menu_size_small"
                android:title="小（10号）"/>
            <item
                android:id="@+id/menu_size_medium"
                android:title="中（16号）"/>
            <item
                android:id="@+id/menu_size_large"
                android:title="大（20号）"/>
        </menu>
    </item>

    <!-- 普通菜单项 -->
    <item
        android:id="@+id/menu_normal"
        android:title="普通菜单项"
        app:showAsAction="never"/>

    <!-- 字体颜色子菜单 -->
    <item
        android:id="@+id/menu_text_color"
        android:title="字体颜色"
        app:showAsAction="never">

        <menu>
            <item
                android:id="@+id/menu_color_red"
                android:title="红色"/>
            <item
                android:id="@+id/menu_color_black"
                android:title="黑色"/>
        </menu>
    </item>

    <!-- 显示自定义对话框菜单项 -->
    <item
        android:id="@+id/menu_show_dialog"
        android:title="显示登录对话框"
        app:showAsAction="never"/>

</menu>